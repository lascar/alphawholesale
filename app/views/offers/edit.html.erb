<h1><%= t("offer.editing") %></h1>

<%= render 'form', offer: @offer, supplier_id: @supplier_id %>
<%= link_to t("show"), path_for(user: @user, path: "offer",
                                options: {object_id: @offer.id}) %> |
<%= link_to t("back"), :back %>
