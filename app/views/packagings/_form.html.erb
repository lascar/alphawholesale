<%= form_for(packaging, local: true) do |form| %>
  <% if packaging.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(packaging.errors.count, "error") %> <%= t("packagings.prohibited_packaging_saved") %></h2>

      <ul>
      <% packaging.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag :packaging %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= label_tag :product %>
    <%= form.select :product_id, @products %>
  </div>

  <% if broker_signed_in? %>
    <div class="field">
      <%= label_tag :supplier %>
      <%= form.select :supplier_id, @suppliers %>
    </div>

    <div class="field">
      <%= form.label :approved %>
      <%= form.check_box :approved %>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
