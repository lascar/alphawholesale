<%= form_for(product, local: true) do |form| %>

  <div class="field">
    <%= form.label :name, t("views.products.product_name") %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit t("views.products.save") %>
  </div>
<% end %>
